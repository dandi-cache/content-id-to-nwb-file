[project]
name = "content-id-to-nwb-file"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "datalad",
    "datalad-container",
    # code/update.py only uses the Python standard library, so no processing dependencies
    # beyond datalad are required.
]
